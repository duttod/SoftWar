<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.softwar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controllers.MainActivity"/>
        <activity android:name=".controllers.CreditActivity" />
        <activity android:name=".controllers.HomePageActivity"/>
        <activity android:name=".controllers.ChargePartie"/>
        <activity android:name=".controllers.CreationNewPartie" />

        </application>
</manifest>